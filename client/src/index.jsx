import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App.jsx';
// <App/> is WHAT i want to render, ('app') is WHERE i want to render.
ReactDOM.render(<App/> , document.getElementById('app'))
